import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

// Rendering the app components inside the div with an id of root
ReactDOM.render(<App/>, document.getElementById('root'));

